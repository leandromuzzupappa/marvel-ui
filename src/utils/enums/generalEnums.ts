export enum ETitleAnimations {
  fadeup = 'FADEUP',
}

// name, comics, stories, series
export enum ESearchAPI {
  characters = 'characters',
  comics = 'comics',
  series = 'series',
  stories = 'stories',
}

export enum ESingleComic {
  characters = 'characters',
  creators = 'creators',
  events = 'events',
  stories = 'stories',
  comics = 'comics',
}

export enum ESingleCreator {
  creators = 'creators',
  comics = 'comics',
  events = 'events',
  series = 'series',
  stories = 'stories',
}

export enum ESingleEvent {
  characters = 'characters',
  comics = 'comics',
  creators = 'creators',
  events = 'events',
  series = 'series',
  stories = 'stories',
}

export enum ESingleSerie {
  characters = 'characters',
  comics = 'comics',
  creators = 'creators',
  events = 'events',
  series = 'series',
  stories = 'stories',
}

export enum ESingleStorie {
  characters = 'characters',
  comics = 'comics',
  creators = 'creators',
  events = 'events',
  series = 'series',
  //stories = 'stories',
}

export enum ESearchQueryNames {
  characters = 'nameStartsWith',
  comics = 'titleStartsWith',
  series = 'titleStartsWith',
  stories = 'titleStartsWith',
}
