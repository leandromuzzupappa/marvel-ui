export enum ETitleAnimations {
  fadeup = 'FADEUP',
}

// name, comics, stories, series
export enum ESearchAPI {
  characters = 'characters',
  comics = 'comics',
  series = 'series',
  stories = 'stories',
}

export enum ESearchQueryNames {
  characters = 'nameStartsWith',
  comics = 'titleStartsWith',
  series = 'titleStartsWith',
  stories = 'titleStartsWith',
}
